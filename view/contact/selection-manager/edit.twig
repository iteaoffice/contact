{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-edit-selection-%s")|format(selection)) %}

<h1 class="display-4">{{ translate("txt-edit-selection-%s")|format(selection) }} </h1>

{% include 'contact/partial/form/selection' %}