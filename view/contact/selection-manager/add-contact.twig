{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-add-contact-to-selection")) %}

<h1 class="display-4">{{ translate("txt-add-contact-to-selection") }} </h1>


{% do form.prepare() %}
{{ form().openTag(form)|raw }}

<div class="form-group">
    <label class="col-sm-3 control-label">{{ translate("txt-contact") }}</label>

    <div class="col-sm-9">
        <p class="form-control-static">{{ contactLink(contact, 'view-admin', 'name') }} {{ selectionLink(selection, 'edit', 'icon') }}</p>
    </div>
</div>


{{ ztbformelement(form.get('selection')) }}


<div class="form-group">
    <div class="col-sm-offset-3 col-sm-9">
        {{ ztbformelement(form.get('submit')) }}
        {{ ztbformelement(form.get('cancel')) }}
    </div>
</div>

{{ form().closeTag()|raw }}