<h1>{{ translate("txt-subarea-list-title") }}</h1>
<p class="lead">{{ translate("txt-subarea-list-explanation") }}</p>

{% if subAreas|length > 0 %}
    <table class="table table-striped">
        <thead>
        <tr>
            <th>{{ translate("txt-name") }}</th>
            <th>{{ translate("txt-label") }}</th>
            <th>{{ translate("txt-amount-of-equipment") }}</th>
        </tr>
        </thead>
        <tfoot>
        </tfoot>
        <tbody>
        {% for subArea in subAreas %}
            <tr>
                <td>
                    {{ subAreaLink(subArea, 'view', 'name') }}
                    {{ subAreaLink(subArea, 'edit', 'icon') }}
                </td>
                <td>{{ subAreaLink(subArea, 'view', 'label') }}</td>
                <td>{{ subArea.equipment.count }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% else %}
    {{ ztbalert.info(translate("txt-no-sub-areas-found"))|raw }}
{% endif %}
