{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-new-opt-in")) %}

<h1 class="display-4">{{ translate("txt-new-opt-in") }}</h1>

{% include 'contact/partial/form/opt-in' %}
