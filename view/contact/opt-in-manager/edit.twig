{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-edit-opt-in")) %}

<h1>{{ translate("txt-edit-opt-in") }}</h1>

{% include 'contact/partial/form/opt-in' %}
