{% do headTitle().append(translate("txt-upload-nda-title")) %}

<h1>{{ translate("txt-edit-dnd-for-contact-%s")|format(contact.parseFullName())|raw }}</h1>

{% do form.prepare() %}
{{ form().openTag(form)|raw }}

<div class="row mb-3">
    <label class="col-sm-3 col-form-label">{{ translate("txt-contact") }}</label>

    <div class="col-sm-9">
        <p class="form-control-plaintext">{{ contactLink(contact,'view-admin','name') }}</p>
    </div>
</div>

<div class="row mb-3">
    <label class="col-sm-3 col-form-label">{{ translate("txt-download-nda") }}</label>

    <div class="col-sm-9">
        <p class="form-control-plaintext">{{ dndLink(dnd, 'download','text') }}</p>
    </div>
</div>

{{ lbs5formelement(form.get('contact_entity_dnd').get('program')) }}
{{ lbs5formelement(form.get('contact_entity_dnd').get('file')) }}
{{ formelement(form.get('csrf')) }}

<div class="row mb-3">
    <div class="offset-sm-3 col-sm-9">

        {{ lbs5formelement(form.get('submit')) }}
        {% if form.has('delete') %}
            {{ lbs5formelement(form.get('delete')) }}
        {% endif %}
        {{ lbs5formelement(form.get('cancel')) }}
    </div>
</div>

{{ form().closeTag()|raw }}
